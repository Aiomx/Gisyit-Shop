/**
 * Stripe MCP Module
 * 
 * This module provides utilities for interacting with Stripe MCP.
 * 
 * Usage:
 * - Import types from '~/lib/stripe/types'
 * - Import checkout utilities from '~/lib/stripe/checkout.server' (server-side only)
 * - Import MCP client from '~/lib/stripe/mcp-client.server' (server-side only)
 * - Import webhook utilities from '~/lib/stripe/webhook.server' (server-side only)
 */

// Re-export all types
export * from "./types";
